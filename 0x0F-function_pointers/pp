#include "function_pointers.h"

/**
 * print_name - prints name as it is
 * @print a name
 * @name:argument passed to the function
 * @print_fun - place holder that prints
 * Return:return nothing
 */

void print_name(char *name, void (*f)(char *))
{
	f(name);
}
